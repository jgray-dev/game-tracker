function displayProgress(game, progress) {
  const div = document.querySelector(".game-1 #progress-bar")
  const bar = document.createElement("div")
  bar.id = `bar-${game}`
  div.innerHTML = ""
  
}

displayProgress()